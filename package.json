{
  "private": true,
  "eslintConfig": {
    "extends": "@wildpeaks/typescript"
  },
  "eslintIgnore": [
    "test/fixtures"
  ],
  "prettier": "@wildpeaks/prettier-config",
  "scripts": {
    "build": "mocha -r ts-node/register src/build.ts",
    "lint": "eslint src/*.ts test/*.ts",
    "test:postbuild": "mocha -r ts-node/register test/*.test.ts"
  },
  "devDependencies": {
    "@types/mocha": "9.1.1",
    "@types/node": "18.6.2",
    "@wildpeaks/eslint-config-typescript": "15.5.0",
    "@wildpeaks/prettier-config": "15.5.0",
    "@wildpeaks/tsconfig": "4.0.0",
    "eslint": "8.21.0",
    "fs-extra": "10.1.0",
    "mocha": "10.0.0",
    "ts-node": "10.9.1",
    "typescript": "4.7.4"
  }
}
