{
  "private": true,
  "eslintConfig": {
    "extends": "@wildpeaks/typescript"
  },
  "eslintIgnore": [
    "test/fixtures"
  ],
  "prettier": "@wildpeaks/prettier-config",
  "scripts": {
    "build": "mocha -r ts-node/register src/build.ts",
    "lint": "eslint src/*.ts test/*.ts",
    "test:postbuild": "mocha -r ts-node/register test/*.test.ts"
  },
  "devDependencies": {
    "@types/mocha": "8.2.3",
    "@types/node": "16.0.1",
    "@wildpeaks/eslint-config-typescript": "12.2.0",
    "@wildpeaks/prettier-config": "12.2.0",
    "@wildpeaks/tsconfig": "4.0.0",
    "eslint": "7.30.0",
    "fs-extra": "10.0.0",
    "mocha": "9.0.2",
    "ts-node": "10.0.0",
    "typescript": "4.3.5"
  }
}
