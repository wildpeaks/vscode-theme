{
  "private": true,
  "eslintConfig": {
    "extends": "@wildpeaks/typescript"
  },
  "eslintIgnore": [
    "test/fixtures"
  ],
  "prettier": "@wildpeaks/prettier-config",
  "scripts": {
    "build": "mocha -r ts-node/register src/build.ts",
    "lint": "eslint src/*.ts test/*.ts",
    "test:postbuild": "mocha -r ts-node/register test/*.test.ts"
  },
  "devDependencies": {
    "@types/mocha": "9.0.0",
    "@types/node": "16.11.13",
    "@wildpeaks/eslint-config-typescript": "15.1.0",
    "@wildpeaks/prettier-config": "15.1.0",
    "@wildpeaks/tsconfig": "4.0.0",
    "eslint": "8.5.0",
    "fs-extra": "10.0.0",
    "mocha": "9.1.3",
    "ts-node": "10.4.0",
    "typescript": "4.5.4"
  }
}
