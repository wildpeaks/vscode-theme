{
  "private": true,
  "eslintConfig": {
    "extends": "@wildpeaks/typescript"
  },
  "eslintIgnore": [
    "themes",
    "test/fixtures"
  ],
  "prettier": "@wildpeaks/prettier-config",
  "scripts": {
    "build": "mocha -r ts-node/register src/build.ts --timeout 10000 --slow 5000",
    "lint": "eslint src/**/*.ts",
    "test": "mocha -r ts-node/register test/unit/*.test.ts"
  },
  "devDependencies": {
    "@types/mocha": "8.2.0",
    "@types/node": "14.14.22",
    "@wildpeaks/eslint-config-typescript": "11.0.0",
    "@wildpeaks/prettier-config": "11.0.0",
    "@wildpeaks/tsconfig": "4.0.0-rc2",
    "eslint": "7.19.0",
    "fs-extra": "9.1.0",
    "mocha": "8.2.1",
    "ts-node": "9.1.1",
    "typescript": "4.1.3"
  }
}
